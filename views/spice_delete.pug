extends layout

block content
  h1 #{title}: #{spice.name}

  p Do you really want to delete this Item?

    form(method='POST' action='')
      div.form-group
        input#spiceid.form-control(type='hidden',name='spiceid', required='true', value=spice._id )

      button.btn.btn-primary(type='submit') Delete Item
